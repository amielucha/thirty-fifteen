<nav class="navigation pagination" role="navigation">
	<div class="nav-links">
    {{#if prev}}
        <a class="prev page-numbers" href="{{page_url prev}}">&larr; Newer Posts</a>
    {{/if}}
    <span class="page-numbers current">Page {{page}} of {{pages}}</span>
    {{#if next}}
        <a class="next page-numbers" href="{{page_url next}}">Older Posts &rarr;</a>
    {{/if}}
	</div>
</nav>
